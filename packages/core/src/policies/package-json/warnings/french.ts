/* eslint-disable max-len */

// Import Internal Dependencies
import { taggedString } from "../../../utils.js";

export const french = Object.freeze({
  TYPE: taggedString`Propriété <type> invalide, on s'attendait à '${"expectedType"}' mais on a obtenu '${"type"}' à la place`
});
